pip install -r requirements.txt
Requirements shows all packages to install

Python 3 is used for this project

.env must include the following to access ONYX DB: USER, PASSWORD, HOST, DB

credentials.py must be saved to src folder and includes the following to access APIs: MAPBOX_ACCESS_TOKEN, COSTQUEST_USER_ID, COSTQUEST_PASS